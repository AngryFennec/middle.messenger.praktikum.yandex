export default interface InputOptions {
  inputClass?: string;
  inputType: string;
  inputPlaceholder?: string;
  inputValue?: string;
  inputName: string;
  labelText?: string;
  validation?: string;
}
