export default `<label class="input {{inputClass}}">{{labelText}}
<input type="{{inputType}}" required placeholder="{{inputPlaceholder}}" value="{{inputValue}}" name="{{inputName}}" data-validation="{{validation}}" class="input__input">
</label>`;
