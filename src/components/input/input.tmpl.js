export default `<label class="input {{inputClass}}">{{labelText}}
<input type="{{inputType}}" placeholder="{{inputPlaceholder}}" value="{{inputValue}}" name="{{inputName}}" class="input__input">
</label>`
